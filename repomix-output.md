This file is a merged representation of the entire codebase, combining all repository files into a single document.
Generated by Repomix on: 2026-03-22 18:51:11

# File Summary

## Purpose:

This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.

## File Format:

The content is organized as follows:
1. This summary section
2. Repository information
3. Repository structure
4. Multiple file entries, each consisting of:
   a. A header with the file path (## File: path/to/file)
   b. The full contents of the file in a code block

## Usage Guidelines:

- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.

## Notes:

- Some files may have been excluded based on .gitignore rules and Repomix's
  configuration.
- Binary files are not included in this packed representation. Please refer to
  the Repository Structure section for a complete list of file paths, including
  binary files.

## Additional Information:

For more information about Repomix, visit: https://github.com/andersonby/python-repomix


# Repository Structure

```
eslint.config.mjs
next.config.ts
open-next.config.ts
package.json
postcss.config.js
public
  _headers
src
  components
    IconMap.tsx
    Logo.tsx
    Spotlight.tsx
  data
    landingData.json
  hooks
    index.ts
    useAnimation.ts
    useMousePosition.ts
    useScrolled.ts
  pages
    index.tsx
    _app.tsx
  sections
    Contact
      ContactForm.tsx
      index.tsx
      InfoCard.tsx
      types.ts
    Footer
      FooterLinkGroup.tsx
      index.tsx
      types.ts
    Hero
      HeroAsset.tsx
      index.tsx
      types.ts
    Lab
      index.tsx
      Newsletter.tsx
      types.ts
    Methodology
      index.tsx
      MethodStep.tsx
      types.ts
    Navigation
      index.tsx
      types.ts
    Pricing
      index.tsx
      PricingCard.tsx
      PricingToggle.tsx
      types.ts
    Services
      index.tsx
      ServiceCard.tsx
      types.ts
  styles
    animations.css
    globals.css
tailwind.config.js
tsconfig.json
wrangler.jsonc
```

# Repository Files


## eslint.config.mjs

```text
import { dirname } from "path";
import { fileURLToPath } from "url";
import { FlatCompat } from "@eslint/eslintrc";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({
  baseDirectory: __dirname,
});

const eslintConfig = [
  ...compat.extends("next/core-web-vitals", "next/typescript"),
  {
    rules: {
      "@next/next/no-css-tags": "off",
    },
  },
];

export default eslintConfig;
```

## next.config.ts

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
};

export default nextConfig;

// added by create cloudflare to enable calling `getCloudflareContext()` in `next dev`
import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";
initOpenNextCloudflareForDev();
```

## open-next.config.ts

```typescript
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
	// Uncomment to enable R2 cache,
	// It should be imported as:
	// `import r2IncrementalCache from "@opennextjs/cloudflare/overrides/incremental-cache/r2-incremental-cache";`
	// See https://opennext.js.org/cloudflare/caching for more details
	// incrementalCache: r2IncrementalCache,
});
```

## package.json

```json
{
	"name": "landing-page",
	"description": "Build a full-stack web application with Next.js.",
	"cloudflare": {
		"label": "Next.js Framework Starter",
		"products": [
			"Workers"
		],
		"categories": [],
		"preview_icon_url": "https://imagedelivery.net/wSMYJvS3Xw-n339CbDyDIA/05eee9e0-2c04-48fd-11bf-84e65f839000/public",
		"preview_image_url": "https://imagedelivery.net/wSMYJvS3Xw-n339CbDyDIA/4e913a56-fd99-4372-0586-9f1364173100/preview",
		"publish": true
	},
	"dependencies": {
		"@opennextjs/cloudflare": "1.14.0",
		"lucide-react": "^0.453.0",
		"next": "16.0.7",
		"react": "19.2.1",
		"react-dom": "19.2.1",
		"tailwindcss-animate": "^1.0.7"
	},
	"devDependencies": {
		"@eslint/eslintrc": "3",
		"@types/node": "24.10.1",
		"@types/react": "19.2.7",
		"@types/react-dom": "19.2.3",
		"autoprefixer": "^10.4.20",
		"eslint": "9.39.2",
		"eslint-config-next": "16.0.7",
		"postcss": "^8.4.49",
		"tailwindcss": "^3.4.17",
		"typescript": "5.9.3",
		"wrangler": "4.56.0"
	},
	"private": true,
	"scripts": {
		"build": "next build",
		"cf-typegen": "wrangler types --env-interface CloudflareEnv env.d.ts",
		"check": "npm run build && tsc",
		"deploy": "opennextjs-cloudflare build && opennextjs-cloudflare deploy",
		"dev": "next dev",
		"lint": "next lint",
		"preview": "opennextjs-cloudflare build && opennextjs-cloudflare preview",
		"start": "next start"
	}
}
```

## postcss.config.js

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

## public/_headers

```text
# https://developers.cloudflare.com/workers/static-assets/headers
/_next/static/*
  Cache-Control: public,max-age=31536000,immutable
```

## src/components/IconMap.tsx

```text
import { Cpu, Layers, Zap, Target } from "lucide-react";

export const iconMap = {
  Cpu,
  Layers,
  Zap,
  Target,
};

export type IconName = keyof typeof iconMap;
```

## src/components/Logo.tsx

```text
export const Logo = () => (
  <div className="flex items-center gap-4 group">
    <svg
      width="44"
      height="44"
      viewBox="0 0 100 100"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      className="transition-transform duration-700 group-hover:scale-105"
    >
      <defs>
        <linearGradient id="hexGrad" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" stopColor="#1C058E" />
          <stop offset="100%" stopColor="#0A0A0A" />
        </linearGradient>
        <linearGradient id="curveGrad" x1="0%" y1="100%" x2="100%" y2="0%">
          <stop offset="0%" stopColor="#1C058E" />
          <stop offset="100%" stopColor="#FDFDFD" />
        </linearGradient>
      </defs>
      {/* Contenedor Hexagonal */}
      <polygon points="50,5 89,27.5 89,72.5 50,95 11,72.5 11,27.5" fill="url(#hexGrad)" stroke="#1C058E" strokeWidth="1.5" />
      
      {/* F & L Entrelazadas */}
      <path d="M 40 32 L 60 32 M 40 48 L 52 48 M 40 32 L 40 68" stroke="#FDFDFD" strokeWidth="5" strokeLinecap="square" />
      <path d="M 56 42 L 56 68 L 72 68" stroke="#FDFDFD" strokeWidth="5" strokeLinecap="square" opacity="0.9" />
      
      {/* Curva Dinámica (Escalabilidad) */}
      <path d="M 20 80 Q 50 40 80 20" stroke="url(#curveGrad)" strokeWidth="3.5" strokeLinecap="round" fill="none" className="drop-shadow-md" />
    </svg>
    <div className="flex flex-col justify-center">
      <span className="font-conthrax font-semibold text-lg leading-none tracking-widest text-fusa-white">FUSA</span>
      <span className="font-conthrax font-bold text-[10px] leading-tight tracking-[0.35em] text-fusa-indigo mt-0.5">
        LABS
      </span>
    </div>
  </div>
);
```

## src/components/Spotlight.tsx

```text
interface SpotlightProps {
  mousePos: { x: number; y: number };
}

export const Spotlight = ({ mousePos }: SpotlightProps) => (
  <div
    className="fixed inset-0 pointer-events-none z-0"
    style={{
      background: `radial-gradient(1000px circle at ${mousePos.x}px ${mousePos.y}px, rgba(255,255,255,0.035), transparent 80%)`,
    }}
  />
);
```

## src/data/landingData.json

```json
{
  "branding": {
    "name": "Modeling & Researching Labs",
    "suffix": "LAB",
    "tagline": "© 2026 MR® LAB SYSTEM",
    "footer": "MR LAB / 2026"
  },
  "navigation": {
    "language": "ESPAÑOL",
    "menuItems": ["Inicio", "Lab", "Productos", "Proyectos", "Blog"],
    "ctaButton": "Contacto"
  },
  "hero": {
    "tags": [
      "Redes Multiagente",
      "Automatización de Procesos",
      "SaaS con IA",
      "Implementación"
    ],
    "title": "MR",
    "subtitle": "/ Lab",
    "description": "Creamos, escalamos y optimizamos soluciones con redes de agentes de IA que simplifican tu forma de trabajar e integran inteligencia en tu stack actual.",
    "highlightedText": "redes de agentes de IA",
    "videoUrl": "https://framerusercontent.com/assets/O5EOvvLBC8yOJKwEcDAmQg5gU.mp4"
  },
  "services": {
    "sectionNumber": "(02)",
    "sectionLabel": "Qué hacemos",
    "title": "Nuestros Servicios.",
    "subtitle": "Soluciones de IA a medida para resolver desafíos empresariales específicos.",
    "items": [
      {
        "num": "001",
        "title": "Desarrollo de Agentes de IA",
        "description": "Agentes a medida diseñados para resolver desafíos específicos, optimizar flujos de trabajo y aumentar la eficiencia automatizada.",
        "tags": ["Agente IA", "Automatización", "Integración"],
        "icon": "Cpu"
      },
      {
        "num": "002",
        "title": "Automatización con IA Personalizada",
        "description": "Eliminamos tareas repetitivas y nos integramos sin problemas con tus sistemas existentes para aumentos medibles en productividad.",
        "tags": ["Optimización", "Flujos de trabajo", "ROI"],
        "icon": "Layers"
      },
      {
        "num": "003",
        "title": "Aplicaciones SaaS por Sector",
        "description": "Soluciones de IA adaptadas a los requisitos únicos de tu industria, utilizando nuestras plataformas de software propietarias.",
        "tags": ["SaaS", "Específico", "Cloud"],
        "icon": "Zap"
      },
      {
        "num": "004",
        "title": "Consultoría en Implementación",
        "description": "Orientación estratégica para navegar tu trayectoria de adopción de IA, desde la evaluación hasta la optimización continua.",
        "tags": ["Estrategia", "Planning", "Transformación"],
        "icon": "Target"
      }
    ]
  },
  "methodology": {
    "sectionLabel": "Sobre nosotros",
    "title": "Cómo construimos el impacto.",
    "description": "No entrenamos modelos aislados. Integramos IA avanzada conectada a tu stack tecnológico actual.",
    "steps": [
      {
        "num": "01",
        "title": "Análisis estratégico",
        "description": "Identificamos oportunidades de automatización con IA de alto ROI."
      },
      {
        "num": "02",
        "title": "Soluciones personalizadas",
        "description": "Arquitectura que se integra perfectamente con tu ecosistema tecnológico."
      },
      {
        "num": "03",
        "title": "Implementación flexible",
        "description": "Despliegue en nuestra infraestructura segura o en tus propios servidores."
      },
      {
        "num": "04",
        "title": "Optimización & Resultados",
        "description": "Testeo continuo con métricas reales y resultados medibles."
      }
    ]
  },
  "pricing": {
    "badge": "Productos internos",
    "title": "Precios.",
    "modes": {
      "suscripcion": {
        "label": "Suscripción Mensual",
        "card1": {
          "title": "Soluciones tipo SaaS",
          "description1": "Suscripción mensual flexible. Vuelve cuando quieras, todo estará ahí.",
          "description2": "Sin compromisos de largo plazo. Cancela cuando lo necesites."
        },
        "card2": {
          "title": "Modelo de precios basado en subscripciónes",
          "subtitle": "Contactate para opciones de precios personalizadas",
          "priceFrom": "$150 -",
          "priceTo": "personalizado",
          "period": "/mes",
          "features": ["Soluciones a partir de $150"],
          "deliveryLabel": "Tiempo de entrega",
          "deliveryTime": "Recurrente",
          "ctaButton": "Contactate"
        }
      },
      "instalacion": {
        "label": "Instalación",
        "card1": {
          "title": "¿Buscas un desarrollo a medida?",
          "description": "Contáctanos para definir el alcance de tu proyecto y recibir una propuesta de presupuesto personalizada."
        },
        "card2": {
          "price": "$1,000 - 5,000",
          "period": "/instalación",
          "features": [
            "Implementación y personalización",
            "Integración con el stack tecnológico de tu organización"
          ],
          "deliveryLabel": "Tiempo de entrega",
          "deliveryTime": "3-4 semanas",
          "ctaButton": "Contactate"
        }
      }
    }
  },
  "lab": {
    "badge": "Innovation Lab",
    "title": "Lab de Ideas.",
    "description": "Donde exploramos el límite de lo posible. Proyectos experimentales y prototipos. I+D",
    "ctaButton": "Explorar el Journal",
    "newsletter": {
      "label": "Newsletter",
      "placeholder": "Tu email"
    }
  },
  "contact": {
    "title": "Tenés un proyecto en mente?",
    "greeting": "Hola.",
    "description": "Contanos sobre tu proyecto—ya sea que necesites una llamada de descubrimiento, desarrollo personalizado, consultoría estratégica, o que estés interesado en uno de nuestros Productos de IA.",
    "form": {
      "nameLabel": "Tu nombre*",
      "namePlaceholder": "Juan Ramirez",
      "emailLabel": "Correo electrónico*",
      "emailPlaceholder": "juan.ramirez@site.com",
      "messageLabel": "Mensaje",
      "messagePlaceholder": "Tu mensaje",
      "submitButton": "Enviar mensaje",
      "terms": "Al enviar, aceptas nuestros Términos y Política de Privacidad."
    },
    "cards": [
      {
        "icon": "Zap",
        "title": "Respuesta rápida.",
        "description": "Si estás listo para crear y colaborar, nos encantaría saber de ti. Te responderemos en breve."
      },
      {
        "icon": "CheckCircle",
        "title": "Aclarar los próximos pasos.",
        "description": "Después de la reunión inicial, te proporcionaremos un plan detallado, presupuesto y cronograma."
      }
    ]
  },
  "footer": {
    "social": ["Twitter", "Github", "Mail"],
    "navigation": {
      "title": "Navegación",
      "links": ["Inicio", "Lab", "Productos", "Proyectos", "Blog"]
    },
    "legal": {
      "title": "Legal",
      "links": ["Privacidad", "Términos", "Cookies"]
    },
    "location": {
      "title": "Ubicación",
      "text": "Sede en Argentina.\nFormosa."
    },
    "copyright": "© 2026 MR LAB® — ALL RIGHTS RESERVED",
    "status": {
      "text": "Ronda de inversión abierta",
      "active": true
    }
  }
}
```

## src/hooks/index.ts

```typescript
export { useScrolled } from "./useScrolled";
export { useMousePosition } from "./useMousePosition";
export { useAnimation } from "./useAnimation";
```

## src/hooks/useAnimation.ts

```typescript
import { useEffect, useState } from "react";

export const useAnimation = () => {
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    setIsVisible(true);
  }, []);

  return isVisible;
};
```

## src/hooks/useMousePosition.ts

```typescript
import { useEffect, useState } from "react";

export const useMousePosition = () => {
  const [mousePos, setMousePos] = useState({ x: 0, y: 0 });

  useEffect(() => {
    const handleMouseMove = (e: MouseEvent) => {
      setMousePos({ x: e.clientX, y: e.clientY });
    };
    window.addEventListener("mousemove", handleMouseMove);
    return () => window.removeEventListener("mousemove", handleMouseMove);
  }, []);

  return mousePos;
};
```

## src/hooks/useScrolled.ts

```typescript
import { useEffect, useState } from "react";

export const useScrolled = (threshold = 50) => {
  const [isScrolled, setIsScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setIsScrolled(window.scrollY > threshold);
    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, [threshold]);

  return isScrolled;
};
```

## src/pages/index.tsx

```text
import { useScrolled, useMousePosition, useAnimation } from "../hooks";
import { Spotlight } from "../components/Spotlight";
import { Navigation } from "../sections/Navigation";
import { Hero } from "../sections/Hero";
import { Services } from "../sections/Services";
import { Methodology } from "../sections/Methodology";
import { Pricing } from "../sections/Pricing";
import { Lab } from "../sections/Lab";
import { Contact } from "../sections/Contact";
import { Footer } from "../sections/Footer";

import landingData from "../data/landingData.json";

export default function Home() {
  const isScrolled = useScrolled();
  const mousePos = useMousePosition();
  const isVisible = useAnimation();

  return (
    <div className="relative min-h-screen bg-fusa-black text-fusa-white selection:bg-fusa-indigo selection:text-white font-inter antialiased overflow-x-hidden">
      <Spotlight mousePos={mousePos} />
      <Navigation
        isScrolled={isScrolled}
        branding={landingData.branding}
        menuItems={landingData.navigation.menuItems}
        language={landingData.navigation.language}
        ctaButton={landingData.navigation.ctaButton}
      />
      <Hero {...landingData.hero} isVisible={isVisible} />
      <Services
        {...landingData.services}
        items={landingData.services.items as any}
      />
      <Methodology {...landingData.methodology} />{" "}
      <Pricing {...landingData.pricing} />
      <Lab {...landingData.lab} />
      <div className="bg-fusa-white text-fusa-black">
        <Contact {...landingData.contact} />
      </div>
      <Footer {...landingData.footer} branding={landingData.branding} />
    </div>
  );
}
```

## src/pages/_app.tsx

```text
import "../styles/globals.css";
import "../styles/animations.css";
import type { AppProps } from "next/app";

export default function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}
```

## src/sections/Contact/ContactForm.tsx

```text
import { ArrowRight } from "lucide-react";

interface ContactFormProps {
  form: {
    nameLabel: string;
    namePlaceholder: string;
    emailLabel: string;
    emailPlaceholder: string;
    messageLabel: string;
    messagePlaceholder: string;
    submitButton: string;
    terms: string;
  };
}

export const ContactForm = ({ form }: ContactFormProps) => (
  <form className="space-y-6">
    <div className="space-y-2">
      <label className="text-xs font-bold uppercase tracking-widest opacity-60">
        {form.nameLabel}
      </label>
      <input
        type="text"
        placeholder={form.namePlaceholder}
        className="w-full bg-zinc-100 border-none rounded-xl px-6 py-4 outline-none focus:ring-2 focus:ring-black/10 transition-all"
      />
    </div>

    <div className="space-y-2">
      <label className="text-xs font-bold uppercase tracking-widest opacity-60">
        {form.emailLabel}
      </label>
      <input
        type="email"
        placeholder={form.emailPlaceholder}
        className="w-full bg-zinc-100 border-none rounded-xl px-6 py-4 outline-none focus:ring-2 focus:ring-black/10 transition-all"
      />
    </div>

    <div className="space-y-2">
      <label className="text-xs font-bold uppercase tracking-widest opacity-60">
        {form.messageLabel}
      </label>
      <textarea
        placeholder={form.messagePlaceholder}
        rows={5}
        className="w-full bg-zinc-100 border-none rounded-xl px-6 py-4 outline-none resize-none focus:ring-2 focus:ring-black/10 transition-all"
      />
    </div>

    <div className="pt-4">
      <button
        type="submit"
        className="w-full bg-black text-white py-5 rounded-full font-bold text-base hover:bg-zinc-800 transition-all flex items-center justify-center gap-3 group"
      >
        <span>{form.submitButton}</span>
        <ArrowRight
          size={20}
          className="group-hover:translate-x-1 transition-transform"
        />
      </button>
      <p className="text-xs text-black/40 mt-4 text-center">
        {form.terms.split("Términos")[0]}
        <a href="#" className="underline hover:text-black/60">
          Términos
        </a>
        {form.terms.split("Términos")[1].split("Política de Privacidad")[0]}
        <a href="#" className="underline hover:text-black/60">
          Política de Privacidad
        </a>
        {form.terms.split("Política de Privacidad")[1]}
      </p>
    </div>
  </form>
);
```

## src/sections/Contact/index.tsx

```text
import { ContactForm } from "./ContactForm";
import { InfoCard } from "./InfoCard";
import type { ContactProps } from "./types";

export const Contact = ({
  title,
  greeting,
  description,
  form,
  cards,
}: ContactProps) => {
  return (
    <div className="grid grid-cols-1 lg:grid-cols-12 gap-16 pt-16 border-t border-black/5">
      {/* Left Column - Contact Form */}
      <div className="lg:col-span-5">
        <h2 className="text-5xl md:text-7xl font-bold tracking-tighter mb-12 leading-[0.9]">
          {title}
        </h2>
        <ContactForm form={form} />
      </div>

      {/* Right Column - Info Cards */}
      <div className="lg:col-span-7 space-y-8">
        <div className="space-y-6">
          <h3 className="text-4xl md:text-5xl font-bold tracking-tighter leading-tight">
            {greeting}
          </h3>
          <p className="text-xl text-black/60 leading-relaxed">{description}</p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 pt-8">
          {cards.map((card, idx) => (
            <InfoCard key={idx} card={card} />
          ))}
        </div>
      </div>
    </div>
  );
};
```

## src/sections/Contact/InfoCard.tsx

```text
import { Zap, CheckCircle } from "lucide-react";
import type { ContactCard } from "./types";

interface InfoCardProps {
  card: ContactCard;
}

export const InfoCard = ({ card }: InfoCardProps) => {
  const IconComponent = card.icon === "Zap" ? Zap : CheckCircle;

  return (
    <div className="bg-zinc-100 rounded-3xl p-8 space-y-4">
      <div className="w-12 h-12 bg-black rounded-full flex items-center justify-center">
        <IconComponent size={20} className="text-white" />
      </div>
      <h4 className="text-2xl font-bold tracking-tight">{card.title}</h4>
      <p className="text-black/60 leading-relaxed">{card.description}</p>
    </div>
  );
};
```

## src/sections/Contact/types.ts

```typescript
export interface ContactCard {
  icon: string;
  title: string;
  description: string;
}

export interface ContactProps {
  title: string;
  greeting: string;
  description: string;
  form: {
    nameLabel: string;
    namePlaceholder: string;
    emailLabel: string;
    emailPlaceholder: string;
    messageLabel: string;
    messagePlaceholder: string;
    submitButton: string;
    terms: string;
  };
  cards: ContactCard[];
}
```

## src/sections/Footer/FooterLinkGroup.tsx

```text
interface FooterLinkGroupProps {
  title: string;
  links: string[];
}

export const FooterLinkGroup = ({ title, links }: FooterLinkGroupProps) => (
  <div className="flex flex-col gap-8">
    <span className="text-[10px] font-black uppercase tracking-widest opacity-30">
      {title}
    </span>
    <nav className="flex flex-col gap-4 text-sm font-bold opacity-60">
      {links.map((l) => (
        <a
          key={l}
          href="#"
          className="hover:opacity-100 hover:translate-x-1 transition-all"
        >
          {l}
        </a>
      ))}
    </nav>
  </div>
);
```

## src/sections/Footer/index.tsx

```text
import { Twitter, Github, Mail } from "lucide-react";
import { FooterLinkGroup } from "./FooterLinkGroup";
import type { FooterProps } from "./types";

export const Footer = ({ branding, social, navigation, legal, location, copyright, status }: FooterProps) => {
  return (
    <footer className="pt-28 pb-12 px-5 md:px-10 bg-white text-black relative z-10 border-t border-black/5">
      <div className="max-w-[1600px] mx-auto">
        <div className="flex flex-col lg:flex-row justify-between items-start gap-24 mb-32">
          <div className="flex flex-col gap-10">
            <div className="text-2xl font-bold tracking-tighter">
              {branding.footer}
            </div>
            <div className="flex gap-6">
              {social.map((socialItem) => {
                const SocialIcon =
                  socialItem === "Twitter" ? (
                    <Twitter className="opacity-40 hover:opacity-100 transition-opacity cursor-pointer" />
                  ) : socialItem === "Github" ? (
                    <Github className="opacity-40 hover:opacity-100 transition-opacity cursor-pointer" />
                  ) : (
                    <Mail className="opacity-40 hover:opacity-100 transition-opacity cursor-pointer" />
                  );
                return <div key={socialItem}>{SocialIcon}</div>;
              })}
            </div>
          </div>
          <div className="grid grid-cols-2 md:grid-cols-3 gap-20">
            <FooterLinkGroup title={navigation.title} links={navigation.links} />
            <FooterLinkGroup title={legal.title} links={legal.links} />
            <div className="flex flex-col gap-8">
              <span className="text-[10px] font-black uppercase tracking-widest opacity-30">
                {location.title}
              </span>
              <div className="text-sm font-bold opacity-60 leading-relaxed">
                {location.text.split("\n").map((line, i) => (
                  <span key={i}>
                    {line}
                    {i === 0 && <br />}
                  </span>
                ))}
              </div>
            </div>
          </div>
        </div>
        <div className="flex flex-col md:flex-row justify-between items-center gap-10 border-t border-black/5 pt-12">
          <div className="text-[9px] font-black tracking-[0.4em] opacity-20 uppercase">
            {copyright}
          </div>
          <div className="flex items-center gap-3">
            <span
              className={`w-2 h-2 rounded-full ${
                status.active ? "bg-green-500" : "bg-gray-500"
              }`}
            />
            <span className="text-[10px] font-black tracking-widest opacity-40 uppercase">
              {status.text}
            </span>
          </div>
        </div>
      </div>
    </footer>
  );
};
```

## src/sections/Footer/types.ts

```typescript
export interface FooterProps {
  branding: {
    footer: string;
  };
  social: string[];
  navigation: {
    title: string;
    links: string[];
  };
  legal: {
    title: string;
    links: string[];
  };
  location: {
    title: string;
    text: string;
  };
  copyright: string;
  status: {
    active: boolean;
    text: string;
  };
}
```

## src/sections/Hero/HeroAsset.tsx

```text
import { ArrowRight } from "lucide-react";

interface HeroAssetProps {
  videoUrl: string;
  isVisible: boolean;
}

export const HeroAsset = ({ videoUrl, isVisible }: HeroAssetProps) => (
  <div className={`opacity-0 ${isVisible ? "animate-reveal delay-3" : ""}`}>
    <div className="flex flex-col md:flex-row justify-between items-center gap-12">
      <div className="w-full md:w-auto">
        <div className="bg-white text-black p-6 rounded-[2.5rem] flex items-center gap-6 min-w-[340px] shadow-2xl animate-float">
          <div className="w-16 h-16 bg-zinc-900 rounded-2xl overflow-hidden flex-shrink-0 border border-black/5" />
          <div className="flex-1">
            <div className="text-[9px] font-black uppercase tracking-[0.2em] opacity-40">
              lorem ipsum dolor
            </div>
            <div className="text-xl font-bold tracking-tighter">
              lorem ipsum dolor
            </div>
          </div>
          <div className="w-12 h-12 bg-black rounded-full flex items-center justify-center hover:scale-110 transition-transform cursor-pointer shadow-lg">
            <ArrowRight size={20} className="text-white" />
          </div>
        </div>
      </div>

      <div className="relative group w-full lg:w-[500px]">
        <div className="absolute inset-0 bg-white/5 blur-[80px] opacity-10 group-hover:opacity-20 transition-opacity" />
        <div className="relative aspect-video bg-zinc-900/50 rounded-[3rem] border border-white/10 overflow-hidden shadow-2xl transition-all duration-700 hover:scale-[1.01] animate-float [animation-delay:1.5s]">
          <video
            className="w-full h-full object-cover grayscale opacity-50 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-1000"
            src={videoUrl}
            autoPlay
            muted
            loop
            playsInline
          />
          <div className="absolute top-8 right-8 z-20">
            <div className="w-12 h-12 rounded-full bg-white/10 backdrop-blur-xl flex items-center justify-center border border-white/20">
              <ArrowRight size={18} fill="white" />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
);
```

## src/sections/Hero/index.tsx

```text
import { HeroAsset } from "./HeroAsset";
import type { HeroProps } from "./types";

export const Hero = ({
  tags,
  title,
  subtitle,
  description,
  highlightedText,
  videoUrl,
  isVisible,
}: HeroProps) => {
  return (
    <section id="inicio" className="relative pt-36 lg:pt-48 px-5 md:px-10 z-10">
      <div className="max-w-[1600px] mx-auto">
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-10 items-end">
          <div className="lg:col-span-8">
            <div
              className={`flex flex-wrap gap-x-5 gap-y-2 mb-8 opacity-0 ${
                isVisible ? "animate-reveal" : ""
              }`}
            >
              {tags.map((tag) => (
                <span
                  key={tag}
                  className="text-[10px] font-black uppercase tracking-[0.4em] text-white/30 border-l border-white/10 pl-3"
                >
                  {tag}
                </span>
              ))}
            </div>
            <h1
              className={`text-[clamp(3.5rem,13vw,12rem)] font-bold leading-[0.85] tracking-[-0.06em] mb-10 opacity-0 ${
                isVisible ? "animate-reveal delay-1" : ""
              }`}
            >
              {title} <br />
              <span className="text-white/20 italic">{subtitle}</span>
            </h1>
          </div>

          <div
            className={`lg:col-span-4 pb-10 opacity-0 ${
              isVisible ? "animate-reveal delay-2" : ""
            }`}
          >
            <div className="flex flex-col gap-8">
              <p className="text-xl md:text-2xl font-light leading-relaxed text-white/60">
                {description.split(highlightedText)[0]}
                <span className="text-white font-medium">
                  {highlightedText}
                </span>
                {description.split(highlightedText)[1]}
              </p>
              <div className="text-[10px] font-black tracking-[0.5em] text-white/20 uppercase">
                © 2026 MR® LAB SYSTEM
              </div>
            </div>
          </div>
        </div>

        <HeroAsset videoUrl={videoUrl} isVisible={isVisible} />
      </div>
    </section>
  );
};
```

## src/sections/Hero/types.ts

```typescript
export interface HeroProps {
  tags: string[];
  title: string;
  subtitle: string;
  description: string;
  highlightedText: string;
  videoUrl: string;
  isVisible: boolean;
}
```

## src/sections/Lab/index.tsx

```text
import { Lightbulb, ArrowRight } from "lucide-react";
import { Newsletter } from "./Newsletter";
import type { LabProps } from "./types";

export const Lab = ({
  badge,
  title,
  description,
  ctaButton,
  newsletter,
}: LabProps) => {
  return (
    <section
      id="lab"
      className="py-28 px-5 md:px-10 bg-white text-black relative z-10 rounded-t-[4rem]"
    >
      <div className="max-w-[1600px] mx-auto">
        <div className="flex flex-col md:flex-row justify-between items-start gap-16 mb-24">
          <div className="max-w-2xl">
            <div className="flex items-center gap-3 mb-8">
              <div className="w-10 h-10 bg-black text-white flex items-center justify-center rounded-full">
                <Lightbulb size={20} />
              </div>
              <span className="text-xs font-black uppercase tracking-[0.3em] opacity-40">
                {badge}
              </span>
            </div>
            <h2 className="text-6xl md:text-8xl font-black tracking-tighter mb-12 leading-[0.85]">
              {title.split(" ").slice(0, 2).join(" ")} <br />{" "}
              {title.split(" ").slice(2).join(" ")}
            </h2>
            <p className="text-2xl font-medium leading-relaxed mb-12 opacity-70">
              {description}
            </p>
            <button className="flex items-center gap-6 group text-xl font-bold">
              <span>{ctaButton}</span>
              <div className="w-14 h-14 bg-black text-white rounded-full flex items-center justify-center group-hover:translate-x-3 transition-transform">
                <ArrowRight />
              </div>
            </button>
          </div>

          <Newsletter
            label={newsletter.label}
            placeholder={newsletter.placeholder}
          />
        </div>
      </div>
    </section>
  );
};
```

## src/sections/Lab/Newsletter.tsx

```text
import { ArrowRight } from "lucide-react";

interface NewsletterProps {
  label: string;
  placeholder: string;
}

export const Newsletter = ({ label, placeholder }: NewsletterProps) => (
  <div className="w-full md:w-1/3 space-y-12">
    <div className="h-px bg-black/10 w-full" />
    <div className="space-y-6">
      <span className="text-[10px] font-black uppercase tracking-widest opacity-40">
        {label}
      </span>
      <div className="flex gap-4">
        <input
          type="text"
          placeholder={placeholder}
          className="bg-zinc-100 border-none rounded-xl px-6 py-4 flex-1 outline-none"
        />
        <button
          className="bg-black text-white p-4 rounded-xl"
          aria-label="Subscribe to newsletter"
        >
          <ArrowRight size={20} />
        </button>
      </div>
    </div>
  </div>
);
```

## src/sections/Lab/types.ts

```typescript
export interface LabProps {
  badge: string;
  title: string;
  description: string;
  ctaButton: string;
  newsletter: {
    label: string;
    placeholder: string;
  };
}
```

## src/sections/Methodology/index.tsx

```text
import { Search, CheckCircle } from "lucide-react";
import { MethodStep } from "./MethodStep";
import type { MethodologyProps } from "./types";

export const Methodology = ({
  sectionLabel,
  title,
  description,
  steps,
}: MethodologyProps) => {
  return (
    <section className="py-28 px-5 md:px-10 bg-[#050505] relative z-10 border-y border-white/5">
      <div className="max-w-[1600px] mx-auto grid grid-cols-1 lg:grid-cols-12 gap-16">
        <div className="lg:col-span-5">
          <span className="text-[10px] font-black uppercase tracking-[0.5em] text-white/30 mb-8 block">
            {sectionLabel}
          </span>
          <h2 className="text-5xl md:text-7xl font-bold tracking-tighter mb-12 leading-[0.9]">
            {title.split(" ").slice(0, 2).join(" ")} <br />
            {title.split(" ").slice(2).join(" ")}
          </h2>
          <p className="text-white/40 text-xl leading-relaxed mb-10">
            {description}
          </p>
          <div className="flex gap-4">
            <div className="w-12 h-12 rounded-full border border-white/10 flex items-center justify-center animate-pulse">
              <Search size={16} />
            </div>
            <div className="w-12 h-12 rounded-full border border-white/10 flex items-center justify-center">
              <CheckCircle size={16} />
            </div>
          </div>
        </div>

        <div className="lg:col-span-7 flex flex-col gap-12">
          {steps.map((step) => (
            <MethodStep
              key={step.num}
              num={step.num}
              title={step.title}
              desc={step.description}
            />
          ))}
        </div>
      </div>
    </section>
  );
};
```

## src/sections/Methodology/MethodStep.tsx

```text
interface MethodStepProps {
  num: string;
  title: string;
  desc: string;
}

export const MethodStep = ({ num, title, desc }: MethodStepProps) => (
  <div className="flex gap-10 group cursor-default">
    <span className="text-5xl font-bold tracking-tighter text-white/5 group-hover:text-white transition-colors duration-700 leading-none">
      {num}
    </span>
    <div className="space-y-4 flex-1">
      <h4 className="text-2xl font-bold tracking-tight">{title}</h4>
      <p className="text-white/40 text-lg leading-relaxed group-hover:text-white/60 transition-colors">
        {desc}
      </p>
      <div className="h-px w-full bg-white/5 group-hover:bg-white/20 transition-all duration-500" />
    </div>
  </div>
);
```

## src/sections/Methodology/types.ts

```typescript
export interface MethodStep {
  num: string;
  title: string;
  description: string;
}

export interface MethodologyProps {
  sectionLabel: string;
  title: string;
  description: string;
  steps: MethodStep[];
}
```

## src/sections/Navigation/index.tsx

```text
import { Globe } from "lucide-react";
import type { NavigationProps } from "./types";

export const Navigation = ({
  isScrolled,
  branding,
  menuItems,
  language,
  ctaButton,
}: NavigationProps) => {
  const sectionMap: Record<string, string> = {
    Inicio: "#inicio",
    Lab: "#lab",
    Productos: "#productos",
    Proyectos: "#proyectos",
    Blog: "#blog",
  };

  return (
    <nav
      className={`fixed top-0 w-full z-[90] transition-all duration-700 ${
        isScrolled
          ? "py-3 bg-black/80 backdrop-blur-2xl border-b border-white/5 shadow-2xl"
          : "py-8"
      }`}
    >
      <div className="max-w-[1600px] mx-auto px-5 md:px-10 flex items-center justify-between">
        <div className="flex items-center gap-3 font-bold text-xl tracking-tighter group cursor-pointer">
          <span className="tracking-[0.15em] text-[18px]">{branding.name}</span>
          <span className="text-white/20">/</span>
          <span className="tracking-[0.15em] text-[18px]">
            {branding.suffix}
          </span>
        </div>

        <div className="hidden lg:flex items-center gap-14 text-[10px] font-bold tracking-[0.25em] text-white/40 uppercase">
          {menuItems.map((item) => (
            <a
              key={item}
              href={sectionMap[item] || "#"}
              className="hover:text-white transition-colors relative group"
            >
              {item}
              <span className="absolute -bottom-1 left-0 w-0 h-px bg-white transition-all group-hover:w-full" />
            </a>
          ))}
        </div>

        <div className="flex items-center gap-6">
          <div className="hidden sm:flex items-center gap-2 text-[10px] font-bold text-white/30 tracking-widest px-4 py-2 border border-white/10 rounded-full">
            <Globe size={12} />
            <span>{language}</span>
          </div>
          <button className="bg-white text-black px-8 py-3 rounded-full text-[12px] font-black uppercase tracking-widest hover:bg-zinc-200 transition-all active:scale-95">
            {ctaButton}
          </button>
        </div>
      </div>
    </nav>
  );
};
```

## src/sections/Navigation/types.ts

```typescript
export interface NavigationProps {
  isScrolled: boolean;
  branding: {
    name: string;
    suffix: string;
  };
  menuItems: string[];
  language: string;
  ctaButton: string;
}
```

## src/sections/Pricing/index.tsx

```text
import { useState } from "react";
import { PricingToggle } from "./PricingToggle";
import { PricingCard } from "./PricingCard";
import type { PricingProps } from "./types";

export const Pricing = ({ badge, title, modes }: PricingProps) => {
  const [pricingMode, setPricingMode] = useState("instalacion");

  return (
    <section id="productos" className="py-28 px-5 md:px-10 relative z-10">
      <div className="max-w-[1600px] mx-auto">
        {/* Header */}
        <div className="mb-16">
          <div className="flex items-center gap-4 mb-6">
            <div className="bg-white px-4 py-1.5 rounded-full flex items-center gap-2">
              <div className="w-3 h-3 bg-black rounded-sm flex items-center justify-center">
                <div className="w-[1px] h-2 bg-white"></div>
                <div className="w-2 h-[1px] bg-white absolute"></div>
              </div>
              <span className="text-black text-[9px] font-black tracking-[0.3em] uppercase">
                {badge}
              </span>
            </div>
          </div>
          <h2 className="text-5xl md:text-8xl font-bold tracking-tighter leading-none mb-6">
            {title}
          </h2>
        </div>

        {/* Pricing Toggle */}
        <PricingToggle
          pricingMode={pricingMode}
          onModeChange={setPricingMode}
          instalacionLabel={modes.instalacion.label}
          suscripcionLabel={modes.suscripcion.label}
        />

        {/* Pricing Cards */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          {pricingMode === "suscripcion" ? (
            <PricingCard mode={modes.suscripcion} type="suscripcion" />
          ) : (
            <PricingCard mode={modes.instalacion} type="instalacion" />
          )}
        </div>
      </div>
    </section>
  );
};
```

## src/sections/Pricing/PricingCard.tsx

```text
import type { PricingMode } from "./types";

interface PricingCardProps {
  mode: PricingMode;
  type: "instalacion" | "suscripcion";
}

export const PricingCard = ({ mode, type }: PricingCardProps) => {
  return (
    <>
      {/* Card 1 - Description */}
      <div className="bg-white/[0.04] rounded-[2rem] p-8 md:p-10 flex flex-col justify-between">
        <div>
          <h3 className="text-xl md:text-2xl font-bold mb-5 text-white leading-tight">
            {mode.card1.title}
          </h3>
          {type === "suscripcion" ? (
            <>
              <p className="text-white/60 text-sm md:text-base leading-relaxed mb-4">
                {mode.card1.description1}
              </p>
              <p className="text-white/60 text-sm md:text-base leading-relaxed">
                {mode.card1.description2}
              </p>
            </>
          ) : (
            <p className="text-white/60 text-sm md:text-base leading-relaxed">
              {mode.card1.description}
            </p>
          )}
        </div>
      </div>

      {/* Card 2 - Pricing */}
      <div className="bg-white/[0.04] rounded-[2rem] p-8 md:p-10 lg:col-span-2 flex flex-col">
        {type === "suscripcion" && (
          <div className="mb-6">
            <h3 className="text-xl md:text-2xl font-bold mb-3 text-white">
              {mode.card2.title}
            </h3>
            <p className="text-white/60 text-sm leading-relaxed">
              {mode.card2.subtitle}
            </p>
          </div>
        )}

        {/* Price */}
        <div className="mb-8">
          {type === "suscripcion" ? (
            <>
              <div className="flex items-baseline gap-2 mb-2">
                <span className="text-5xl md:text-6xl font-bold tracking-tighter text-white">
                  {mode.card2.priceFrom}
                </span>
                <span className="text-3xl md:text-4xl font-semibold text-white/60">
                  {mode.card2.priceTo}
                </span>
              </div>
              <p className="text-white/60 text-base">{mode.card2.period}</p>
            </>
          ) : (
            <>
              <div className="flex items-baseline gap-2 mb-2">
                <span className="text-5xl md:text-6xl font-bold tracking-tighter text-white">
                  {mode.card2.price}
                </span>
              </div>
              <p className="text-white/60 text-base">{mode.card2.period}</p>
            </>
          )}
        </div>

        {/* Features */}
        <div className="space-y-4 mb-10 flex-1">
          {mode.card2.features.map((feature, idx) => (
            <div key={idx} className="flex items-start gap-3">
              <div className="mt-1 w-5 h-5 rounded-full bg-white/10 flex items-center justify-center flex-shrink-0">
                <div className="w-[1.5px] h-2.5 bg-white"></div>
                <div className="w-2.5 h-[1.5px] bg-white absolute"></div>
              </div>
              <span className="text-white text-sm md:text-base">{feature}</span>
            </div>
          ))}
        </div>

        {/* Delivery Time */}
        <div className="border-t border-white/10 pt-6 mb-8">
          <div className="flex justify-between items-center">
            <span className="text-white/60 text-sm md:text-base">
              {mode.card2.deliveryLabel}
            </span>
            <span className="text-white text-sm md:text-base font-semibold">
              {mode.card2.deliveryTime}
            </span>
          </div>
        </div>

        {/* CTA Button */}
        <button className="w-full bg-white text-black py-4 rounded-full font-semibold text-base hover:bg-white/90 transition-all">
          {mode.card2.ctaButton}
        </button>
      </div>
    </>
  );
};
```

## src/sections/Pricing/PricingToggle.tsx

```text
interface PricingToggleProps {
  pricingMode: string;
  onModeChange: (mode: string) => void;
  instalacionLabel: string;
  suscripcionLabel: string;
}

export const PricingToggle = ({
  pricingMode,
  onModeChange,
  instalacionLabel,
  suscripcionLabel,
}: PricingToggleProps) => (
  <div className="flex justify-start mb-10">
    <div className="inline-flex bg-white/[0.04] rounded-full p-1.5">
      <button
        onClick={() => onModeChange("instalacion")}
        className={`px-8 py-3 rounded-full font-semibold text-sm transition-all flex-1 whitespace-nowrap ${
          pricingMode === "instalacion"
            ? "bg-white text-black"
            : "text-white hover:bg-white/5"
        }`}
      >
        {instalacionLabel}
      </button>
      <button
        onClick={() => onModeChange("suscripcion")}
        className={`px-8 py-3 rounded-full font-semibold text-sm transition-all flex-1 whitespace-nowrap ${
          pricingMode === "suscripcion"
            ? "bg-white text-black"
            : "text-white hover:bg-white/5"
        }`}
      >
        {suscripcionLabel}
      </button>
    </div>
  </div>
);
```

## src/sections/Pricing/types.ts

```typescript
export interface PricingMode {
  label: string;
  card1: {
    title: string;
    description?: string;
    description1?: string;
    description2?: string;
  };
  card2: {
    title?: string;
    subtitle?: string;
    price?: string;
    priceFrom?: string;
    priceTo?: string;
    period: string;
    features: string[];
    deliveryLabel: string;
    deliveryTime: string;
    ctaButton: string;
  };
}

export interface PricingProps {
  badge: string;
  title: string;
  modes: {
    instalacion: PricingMode;
    suscripcion: PricingMode;
  };
}
```

## src/sections/Services/index.tsx

```text
import { ServiceCard } from "./ServiceCard";
import { iconMap } from "../../components/IconMap";
import type { ServicesProps } from "./types";

export const Services = ({
  sectionNumber,
  sectionLabel,
  title,
  subtitle,
  items,
}: ServicesProps) => {
  return (
    <section id="proyectos" className="py-28 px-5 md:px-10 relative z-10">
      <div className="max-w-[1600px] mx-auto">
        <div className="flex flex-col md:flex-row md:items-end justify-between gap-8 mb-20">
          <div className="max-w-3xl">
            <span className="text-[10px] font-black uppercase tracking-[0.5em] text-white/30 mb-8 block">
              {sectionNumber} {sectionLabel}
            </span>
            <h2 className="text-5xl md:text-8xl font-bold tracking-tighter leading-none">
              {title.split(" ").slice(0, -1).join(" ")} <br />
              {title.split(" ").slice(-1)}
            </h2>
          </div>
          <p className="text-white/40 text-xl font-light max-w-sm">
            {subtitle}
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
          {items.map((service) => {
            const IconComponent = iconMap[service.icon];
            return (
              <ServiceCard
                key={service.num}
                num={service.num}
                title={service.title}
                desc={service.description}
                tags={service.tags}
                icon={<IconComponent size={28} />}
              />
            );
          })}
        </div>
      </div>
    </section>
  );
};
```

## src/sections/Services/ServiceCard.tsx

```text
import { ReactNode } from "react";

interface ServiceCardProps {
  num: string;
  title: string;
  desc: string;
  tags: string[];
  icon: ReactNode;
}

export const ServiceCard = ({
  num,
  title,
  desc,
  tags,
  icon,
}: ServiceCardProps) => (
  <div className="group relative p-12 rounded-[3.5rem] bg-[#0a0a0a] border border-white/5 transition-all duration-700 h-[500px] flex flex-col justify-between overflow-hidden hover:border-white/20">
    <div className="flex justify-between items-start relative z-10">
      <div className="flex flex-col">
        <span className="text-[10px] font-black text-white/20 uppercase tracking-widest mb-1">
          Service {num}
        </span>
      </div>
      <div className="w-14 h-14 rounded-full bg-white/5 border border-white/10 flex items-center justify-center text-white/40 group-hover:bg-white group-hover:text-black transition-all duration-700">
        {icon}
      </div>
    </div>

    <div className="relative z-10">
      <h3 className="text-4xl md:text-5xl font-bold tracking-tighter mb-8 group-hover:translate-x-2 transition-transform duration-700 leading-tight">
        {title}
      </h3>
      <p className="text-white/40 text-lg leading-relaxed max-w-sm group-hover:text-white/70 transition-colors">
        {desc}
      </p>
      <div className="flex gap-3 mt-10">
        {tags.map((t) => (
          <span
            key={t}
            className="text-[9px] font-black uppercase tracking-widest bg-white/5 px-3 py-1 rounded-full text-white/30 group-hover:text-white/50 group-hover:bg-white/10 transition-colors"
          >
            {t}
          </span>
        ))}
      </div>
    </div>
    <div className="absolute -right-32 -bottom-32 w-[500px] h-[500px] bg-white/[0.01] blur-[100px] rounded-full group-hover:bg-white/[0.03] transition-all duration-1000" />
  </div>
);
```

## src/sections/Services/types.ts

```typescript
import type { IconName } from "../../components/IconMap";

export interface ServiceItem {
  num: string;
  title: string;
  description: string;
  tags: string[];
  icon: IconName;
}

export interface ServicesProps {
  sectionNumber: string;
  sectionLabel: string;
  title: string;
  subtitle: string;
  items: ServiceItem[];
}
```

## src/styles/animations.css

```css
@import url("https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap");

* {
  font-family:
    "Space Grotesk",
    -apple-system,
    BlinkMacSystemFont,
    sans-serif;
}

@keyframes reveal {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes float {
  0% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-15px);
  }
  100% {
    transform: translateY(0px);
  }
}

.animate-reveal {
  animation: reveal 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.animate-float {
  animation: float 6s ease-in-out infinite;
}

.delay-1 {
  animation-delay: 0.2s;
}
.delay-2 {
  animation-delay: 0.4s;
}
.delay-3 {
  animation-delay: 0.6s;
}
```

## src/styles/globals.css

```css
@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap");
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --vp-c-brand-1: #8e4ec6;
  --vp-c-brand-2: #713ca1;
  --vp-c-bg: #ffffff;
  --vp-c-bg-alt: #f6f6f7;
  --vp-c-bg-soft: #f9f9f9;
  --vp-c-border: #e2e2e3;
  --vp-c-text-1: #3c3c43;
  --vp-c-text-2: rgba(60, 60, 67, 0.7);
  --vp-c-text-3: rgba(60, 60, 67, 0.5);
  --font-family-base: "Inter", sans-serif;
}

.dark {
  --vp-c-brand-1: #a855f7;
  --vp-c-brand-2: #bd34fe;
  --vp-c-bg: #1b1b1f;
  --vp-c-bg-alt: #202127;
  --vp-c-bg-soft: #202127;
  --vp-c-border: #3e3e46;
  --vp-c-text-1: rgba(255, 255, 255, 0.87);
  --vp-c-text-2: rgba(235, 235, 245, 0.6);
  --vp-c-text-3: rgba(235, 235, 245, 0.38);
}

body {
  font-family: var(--font-family-base);
}

.vp-glow-hover:hover {
  border-color: var(--vp-c-brand-1);
  box-shadow: 0 0 15px rgba(168, 85, 247, 0.15);
}

/* Abstract Animations */
@keyframes flow-particles {
  0% {
    transform: translate(0, 0) scale(0.5);
    opacity: 0;
  }
  50% {
    opacity: 1;
  }
  100% {
    transform: translate(100px, 50px) scale(1.2);
    opacity: 0;
  }
}

@keyframes pulse-network {
  0%,
  100% {
    stroke-opacity: 0.2;
  }
  50% {
    stroke-opacity: 0.8;
  }
}

@keyframes scan-vertical {
  0% {
    top: 0%;
    opacity: 0;
  }
  50% {
    opacity: 1;
  }
  100% {
    top: 100%;
    opacity: 0;
  }
}

.animate-spin-slow {
  animation: spin 20s linear infinite;
}
.animate-spin-slow-reverse {
  animation: spin 20s linear infinite reverse;
}
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.dot-pattern {
  background-image: radial-gradient(
    circle at 1px 1px,
    var(--vp-c-text-1) 1px,
    transparent 0
  );
  background-size: 30px 30px;
}

.neural-pattern {
  background-image: radial-gradient(var(--vp-c-text-3) 1px, transparent 1px);
  background-size: 20px 20px;
}

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 0 0% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 0 0% 3.9%;
    --primary: 0 0% 9%;
    --primary-foreground: 0 0% 98%;
    --secondary: 0 0% 96.1%;
    --secondary-foreground: 0 0% 9%;
    --muted: 0 0% 96.1%;
    --muted-foreground: 0 0% 45.1%;
    --accent: 0 0% 96.1%;
    --accent-foreground: 0 0% 9%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 89.8%;
    --input: 0 0% 89.8%;
    --ring: 0 0% 3.9%;
    --chart-1: 12 76% 61%;
    --chart-2: 173 58% 39%;
    --chart-3: 197 37% 24%;
    --chart-4: 43 74% 66%;
    --chart-5: 27 87% 67%;
    --radius: 0.5rem;
  }
  .dark {
    --background: 0 0% 3.9%;
    --foreground: 0 0% 98%;
    --card: 0 0% 3.9%;
    --card-foreground: 0 0% 98%;
    --popover: 0 0% 3.9%;
    --popover-foreground: 0 0% 98%;
    --primary: 0 0% 98%;
    --primary-foreground: 0 0% 9%;
    --secondary: 0 0% 14.9%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 14.9%;
    --muted-foreground: 0 0% 63.9%;
    --accent: 0 0% 14.9%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 14.9%;
    --input: 0 0% 14.9%;
    --ring: 0 0% 83.1%;
    --chart-1: 220 70% 50%;
    --chart-2: 160 60% 45%;
    --chart-3: 30 80% 55%;
    --chart-4: 280 65% 60%;
    --chart-5: 340 75% 55%;
  }
}
```

## tailwind.config.js

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ["class"],
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/sections/**/*.{js,ts,jsx,tsx,mdx}", // ← CRÍTICO para tus secciones
  ],
  theme: {
    extend: {
      fontFamily: {
        inter: ["var(--font-inter)", "sans-serif"],
        conthrax: ["Conthrax", "sans-serif"],
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      colors: {
        fusa: {
          black: "#050505",
          white: "#FDFDFD",
          indigo: "#1C058E",
        },
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        chart: {
          1: "hsl(var(--chart-1))",
          2: "hsl(var(--chart-2))",
          3: "hsl(var(--chart-3))",
          4: "hsl(var(--chart-4))",
          5: "hsl(var(--chart-5))",
        },
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};
```

## tsconfig.json

```json
{
	"compilerOptions": {
		"target": "ES2017",
		"lib": ["dom", "dom.iterable", "esnext"],
		"allowJs": true,
		"skipLibCheck": true,
		"strict": true,
		"noEmit": true,
		"esModuleInterop": true,
		"module": "esnext",
		"moduleResolution": "bundler",
		"resolveJsonModule": true,
		"isolatedModules": true,
		"jsx": "react-jsx",
		"incremental": true,
		"plugins": [
			{
				"name": "next"
			}
		],
		"paths": {
			"@/*": ["./src/*"]
		},
		"types": ["./env.d.ts", "node"]
	},
	"include": [
		"next-env.d.ts",
		"**/*.ts",
		"**/*.tsx",
		".next/types/**/*.ts",
		".next/dev/types/**/*.ts"
	],
	"exclude": ["node_modules"]
}
```

## wrangler.jsonc

```text
/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "landing-page",
	"main": ".open-next/worker.js",
	"compatibility_date": "2025-10-08",
	"compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"],
	"assets": {
		"binding": "ASSETS",
		"directory": ".open-next/assets"
	},
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true
}
```

## Statistics

- Total Files: 46
- Total Characters: 465124
- Total Tokens: 0
